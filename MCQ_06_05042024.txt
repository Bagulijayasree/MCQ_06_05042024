1. How many static variables, local variables and instance variables are present in the code given below?

   public class Car (

   private String vehicleNumber;

   private String color;

   private static int numberOfCars = 0;

   public Car(String vehicleNumber, String color) { this, vehicleNumber this.color color; vehicleNumber; }

   public void display(String name) { 
   System.out.println("Owner's name:" + name); 
   System.out.println("Vehicle number: " + VehicleNumber);
   System.out.println("Color: " + color); 
     }
   }

 a.  static variables: 1; instance variables: 2; local variables: 1

 b.  static variables: 3; instance variables: 0; local variables: 2

 c.  static variables: 1; instance variables: 2; local variables: 3

 d.  static variables: 3; instance variables: 2; local variables: 3

Answer : c

2. Which of the below relations can be bidirectional?

   If class A is related to class B by ___ then class B can also be related to class A by ____. 

   a.  Aggregation

   b.  Association

   c.  Both

   d.  None

Answer : b

3. Which of the following statements are true about constructors in Java?

    a.  If constructor for a class is not defined, then the compiler gives compilation error

    b.  The name of the constructor cannot be the same as the class name

    c.  The parameterless constructor of the parent class is invoked implicitly by the child class constructors if super() is not explicitly used in the                     child class constructor

    d.  A call to a parent class constructor must be the first statement inside the child class constructor


Answer : c,d

4. What will be the output of the below code?

   public class Interviewbit {

   public static void main(String s[]) { 

   int a[] = { 12, 15, 16, 17, 19, 23); 

   for (int i= a.length 1; i > 0; i--) { 

   if (i%3!= 0) {
 
   --i; }
  
   System.out.println(a[i]); 

      } 
    }

   }

   a. 23 19 16 15

   b. 23 19 17 16 15

   c. 19 17 15
   
   d. Throws ArrayIndexOutOfBoundsException

Answer : c

5. When an array is passed to a method, what does the method receive ?

  a. The reference of the array

  b. A copy of the array
  
  c. Length of the array

  d. Copy of first element

Answer : a


  

  





